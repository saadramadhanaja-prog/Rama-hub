-- SCRIPT S LOGO ESP V9.2 FIX - BY RAMA
task.wait(2)
local success, err = pcall(function()

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

-- Variabel
local ESP_On = false
local NoClip_On = false
local InfJump_On = false
local Fly_On = false
local FlySpeed = 50
local UpDown = 0
local ESP_Objects = {}
local BodyGyro, BodyVelocity

-- GUI UTAMA
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "S_Logo_GUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.DisplayOrder = 999 -- KUNCI BIAR PALING ATAS
ScreenGui.Parent = game.CoreGui

-- FRAME LOGO BIAR GAMPANG DI DRAG
local LogoFrame = Instance.new("Frame")
LogoFrame.Size = UDim2.new(0, 45, 0, 45)
LogoFrame.Position = UDim2.new(0, 15, 0.5, -22)
LogoFrame.BackgroundTransparency = 1
LogoFrame.Parent = ScreenGui
LogoFrame.ZIndex = 999

-- LOGO S BISA DI KLIK
local Logo = Instance.new("TextButton")
Logo.Size = UDim2.new(1, 0, 1, 0)
Logo.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
Logo.Text = "S"
Logo.TextColor3 = Color3.fromRGB(255, 255, 255)
Logo.TextScaled = true
Logo.Font = Enum.Font.GothamBold
Logo.Parent = LogoFrame
Logo.ZIndex = 1000 -- PALING ATAS
Logo.AutoButtonColor = false
Instance.new("UICorner", Logo).CornerRadius = UDim.new(1, 0)

-- DRAG LOGO - VERSI FIX MOBILE
local dragging = false
local dragStart, startPos

Logo.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPos = LogoFrame.Position
	end
end)

Logo.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = false
	end
end)

UIS.InputChanged:Connect(function(input)
	if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		local delta = input.Position - dragStart
		LogoFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
	end
end)

-- MENU GUI - LEBIH KECIL & RAPIH
local Menu = Instance.new("Frame")
Menu.Size = UDim2.new(0, 200, 0, 320)
Menu.Position = UDim2.new(0, 70, 0.5, -160)
Menu.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
Menu.BackgroundTransparency = 0.2
Menu.Visible = false
Menu.Parent = ScreenGui
Menu.ZIndex = 999
Instance.new("UICorner", Menu).CornerRadius = UDim.new(0, 10)

-- TOMBOL X
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 22, 0, 22)
CloseBtn.Position = UDim2.new(1, -27, 0, 4)
CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 60, 60)
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.new(1,1,1)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextScaled = true
CloseBtn.ZIndex = 1000
CloseBtn.Parent = Menu
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0,4)

-- TITLE
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -30, 0, 25)
Title.Position = UDim2.new(0, 5, 0, 2)
Title.BackgroundTransparency = 1
Title.Text = "S HUB"
Title.TextColor3 = Color3.fromRGB(0, 170, 255)
Title.Font = Enum.Font.GothamBold
Title.TextScaled = true
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.ZIndex = 1000
Title.Parent = Menu

-- FUNGSI BUAT BIKIN TOMBOL
local function CreateButton(text, posY)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(0, 170, 0, 26)
	btn.Position = UDim2.new(0, 15, 0, posY)
	btn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
	btn.Text = text
	btn.TextColor3 = Color3.new(1,1,1)
	btn.Font = Enum.Font.Gotham
	btn.TextScaled = true
	btn.ZIndex = 1000
	btn.Parent = Menu
	Instance.new("UICorner", btn).CornerRadius = UDim.new(0,5)
	return btn
end

local ESPBtn = CreateButton("ESP: OFF", 35)
local NoClipBtn = CreateButton("NoClip: OFF", 65)
local InfJumpBtn = CreateButton("Inf Jump: OFF", 95)
local FlyBtn = CreateButton("Fly: OFF", 125)

-- SLIDER KECEPATAN FLY
local SpeedLabel = Instance.new("TextLabel")
SpeedLabel.Size = UDim2.new(0, 170, 0, 16)
SpeedLabel.Position = UDim2.new(0, 15, 0, 155)
SpeedLabel.BackgroundTransparency = 1
SpeedLabel.Text = "Speed: 50"
SpeedLabel.TextColor3 = Color3.new(1,1,1)
SpeedLabel.Font = Enum.Font.Gotham
SpeedLabel.TextScaled = true
SpeedLabel.ZIndex = 1000
SpeedLabel.Parent = Menu

local SpeedSlider = Instance.new("TextButton")
SpeedSlider.Size = UDim2.new(0, 170, 0, 8)
SpeedSlider.Position = UDim2.new(0, 15, 0, 175)
SpeedSlider.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
SpeedSlider.Text = ""
SpeedSlider.ZIndex = 1000
SpeedSlider.Parent = Menu
Instance.new("UICorner", SpeedSlider).CornerRadius = UDim.new(0,4)

local SliderFill = Instance.new("Frame")
SliderFill.Size = UDim2.new(0.25, 0, 1, 0)
SliderFill.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
SliderFill.BorderSizePixel = 0
SliderFill.ZIndex = 1001
SliderFill.Parent = SpeedSlider
Instance.new("UICorner", SliderFill).CornerRadius = UDim.new(0,4)

-- LIST TELEPORT PLAYERS
local TPLabel = Instance.new("TextLabel")
TPLabel.Size = UDim2.new(0, 170, 0, 18)
TPLabel.Position = UDim2.new(0, 15, 0, 190)
TPLabel.BackgroundTransparency = 1
TPLabel.Text = "Players:"
TPLabel.TextColor3 = Color3.new(1,1,1)
TPLabel.Font = Enum.Font.GothamBold
TPLabel.TextScaled = true
TPLabel.ZIndex = 1000
TPLabel.Parent = Menu

local TPList = Instance.new("ScrollingFrame")
TPList.Size = UDim2.new(0, 170, 0, 75)
TPList.Position = UDim2.new(0, 15, 0, 210)
TPList.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
TPList.BorderSizePixel = 0
TPList.ScrollBarThickness = 3
TPList.CanvasSize = UDim2.new(0, 0, 0, 0)
TPList.ZIndex = 1000
TPList.Parent = Menu
Instance.new("UICorner", TPList).CornerRadius = UDim.new(0,5)

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Parent = TPList
UIListLayout.Padding = UDim.new(0, 3)

-- CREDIT
local Credit = Instance.new("TextLabel")
Credit.Size = UDim2.new(1, 0, 0, 15)
Credit.Position = UDim2.new(0, 0, 1, -17)
Credit.BackgroundTransparency = 1
Credit.Text = "Script by Rama"
Credit.TextColor3 = Color3.fromRGB(0, 170, 255)
Credit.Font = Enum.Font.GothamBold
Credit.TextScaled = true
Credit.ZIndex = 1000
Credit.Parent = Menu

-- FUNGSI SLIDER
local sliding = false
SpeedSlider.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then sliding = true end
end)
UIS.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then sliding = false end
end)
UIS.InputChanged:Connect(function(input)
	if sliding and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		local pos = math.clamp((input.Position.X - SpeedSlider.AbsolutePosition.X) / SpeedSlider.AbsoluteSize.X, 0, 1)
		SliderFill.Size = UDim2.new(pos, 0, 1, 0)
		FlySpeed = math.floor(pos * 200)
		SpeedLabel.Text = "Speed: "..FlySpeed
	end
end)

-- TOMBOL NAIK TURUN KECIL
local UpBtn = Instance.new("TextButton")
UpBtn.Size = UDim2.new(0, 45, 0, 45)
UpBtn.Position = UDim2.new(1, -55, 0.75, -50)
UpBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
UpBtn.Text = "UP"
UpBtn.TextColor3 = Color3.new(1,1,1)
UpBtn.Font = Enum.Font.GothamBold
UpBtn.TextScaled = true
UpBtn.Visible = false
UpBtn.ZIndex = 999
UpBtn.Parent = ScreenGui
Instance.new("UICorner", UpBtn).CornerRadius = UDim.new(0,8)

local DownBtn = Instance.new("TextButton")
DownBtn.Size = UDim2.new(0, 45, 0, 45)
DownBtn.Position = UDim2.new(1, -55, 0.75, 0)
DownBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
DownBtn.Text = "DOWN"
DownBtn.TextColor3 = Color3.new(1,1,1)
DownBtn.Font = Enum.Font.GothamBold
DownBtn.TextScaled = true
DownBtn.Visible = false
DownBtn.ZIndex = 999
DownBtn.Parent = ScreenGui
Instance.new("UICorner", DownBtn).CornerRadius = UDim.new(0,8)

-- FUNGSI CLEAR ESP
local function ClearESP()
	for _,v in pairs(ESP_Objects) do
		if v then v:Destroy() end
	end
	ESP_Objects = {}
end

-- FUNGSI BUAT ESP 1 PLAYER
local function SetupESP(plr)
	if plr == LocalPlayer then return end
	local function onChar(char)
		task.wait(0.5)
		local Highlight = Instance.new("Highlight")
		Highlight.Name = "ESP_Highlight"
		Highlight.Adornee = char
		Highlight.FillColor = Color3.fromRGB(0, 170, 255)
	Highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
	Highlight.FillTransparency = 0.5
	Highlight.OutlineTransparency = 0
	Highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
	Highlight.Parent = game.CoreGui
		table.insert(ESP_Objects, Highlight)
	end
	if plr.Character then onChar(plr.Character) end
	plr.CharacterAdded:Connect(onChar)
end

-- FUNGSI UPDATE LIST TELEPORT
local function UpdateTPList()
	for _,child in pairs(TPList:GetChildren()) do
		if child:IsA("TextButton") then child:Destroy() end
	end
	for _,plr in pairs(Players:GetPlayers()) do
		if plr ~= LocalPlayer then
			local btn = Instance.new("TextButton")
			btn.Size = UDim2.new(1, -8, 0, 22)
			btn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
			btn.Text = plr.Name
			btn.TextColor3 = Color3.new(1,1,1)
			btn.Font = Enum.Font.Gotham
			btn.TextScaled = true
			btn.ZIndex = 1001
			btn.Parent = TPList
			Instance.new("UICorner", btn).CornerRadius = UDim.new(0,3)
			btn.MouseButton1Click:Connect(function()
				if plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") and LocalPlayer.Character then
					LocalPlayer.Character:SetPrimaryPartCFrame(plr.Character.HumanoidRootPart.CFrame + Vector3.new(3,0,0))
				end
			end)
		end
	end
	TPList.CanvasSize = UDim2.new(0, 0, 0, UIListLayout.AbsoluteContentSize.Y)
end

Players.PlayerAdded:Connect(UpdateTPList)
Players.PlayerRemoving:Connect(UpdateTPList)
UpdateTPList()

-- FUNGSI NOCLIP
RunService.Stepped:Connect(function()
	if NoClip_On and LocalPlayer.Character then
		for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
			if part:IsA("BasePart") and part.CanCollide == true then
				part.CanCollide = false
			end
		end
	end
end)

-- FUNGSI INFINITE JUMP
UIS.JumpRequest:Connect(function()
	if InfJump_On and LocalPlayer.Character then
		local Humanoid = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
		if Humanoid then Humanoid:ChangeState(Enum.HumanoidStateType.Jumping) end
	end
end)

-- FUNGSI FLY
local function StartFly()
	if not LocalPlayer.Character then return end
	local HRP = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
	if not HRP then return end
	BodyGyro = Instance.new("BodyGyro")
	BodyGyro.P = 9e4
	BodyGyro.MaxTorque = Vector3.new(9e4, 9e4, 9e4)
	BodyGyro.CFrame = HRP.CFrame
	BodyGyro.Parent = HRP
	BodyVelocity = Instance.new("BodyVelocity")
	BodyVelocity.Velocity = Vector3.new(0,0,0)
	BodyVelocity.MaxForce = Vector3.new(9e4, 9e4, 9e4)
	BodyVelocity.Parent = HRP
	UpBtn.Visible = true
	DownBtn.Visible = true
end

local function StopFly()
	if BodyGyro then BodyGyro:Destroy() BodyGyro = nil end
	if BodyVelocity then BodyVelocity:Destroy() BodyVelocity = nil end
	UpBtn.Visible = false
	DownBtn.Visible = false
	UpDown = 0
end

UpBtn.InputBegan:Connect(function() if Fly_On then UpDown = 1 end)
UpBtn.InputEnded:Connect(function() UpDown = 0 end)
DownBtn.InputBegan:Connect(function() if Fly_On then UpDown = -1 end end)
DownBtn.InputEnded:Connect(function() UpDown = 0 end)

RunService.RenderStepped:Connect(function()
	if Fly_On and BodyVelocity and LocalPlayer.Character then
		local HRP = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
		local Humanoid = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
		if HRP and Humanoid then
			BodyGyro.CFrame = workspace.CurrentCamera.CFrame
			local moveDir = Humanoid.MoveDirection
			local flyVec = Vector3.new(moveDir.X, UpDown, moveDir.Z) * FlySpeed
			BodyVelocity.Velocity = flyVec
		end
	end
end)

-- EVENT TOMBOL MENU - FIX KLIK
Logo.MouseButton1Click:Connect(function()
	Menu.Visible = not Menu.Visible
	UpdateTPList()
end)

CloseBtn.MouseButton1Click:Connect(function() Menu.Visible = false end)

ESPBtn.MouseButton1Click:Connect(function()
	ESP_On = not ESP_On
	ESPBtn.Text = "ESP: "..(ESP_On and "ON" or "OFF")
	ESPBtn.BackgroundColor3 = ESP_On and Color3.fromRGB(50, 255, 50) or Color3.fromRGB(0, 170, 255)
	if ESP_On then
		for _,plr in pairs(Players:GetPlayers()) do SetupESP(plr) end
	Players.PlayerAdded:Connect(SetupESP)
	else ClearESP() end
end)

NoClipBtn.MouseButton1Click:Connect(function()
	NoClip_On = not NoClip_On
	NoClipBtn.Text = "NoClip: "..(NoClip_On and "ON" or "OFF")
	NoClipBtn.BackgroundColor3 = NoClip_On and Color3.fromRGB(50, 255, 50) or Color3.fromRGB(0, 170, 255)
end)

InfJumpBtn.MouseButton1Click:Connect(function()
	InfJump_On = not InfJump_On
	InfJumpBtn.Text = "Inf Jump: "..(InfJump_On and "ON" or "OFF")
	InfJumpBtn.BackgroundColor3 = InfJump_On and Color3.fromRGB(50, 255, 50) or Color3.fromRGB(0, 170, 255)
end)

FlyBtn.MouseButton1Click:Connect(function()
	Fly_On = not Fly_On
	FlyBtn.Text = "Fly: "..(Fly_On and "ON" or "OFF")
	FlyBtn.BackgroundColor3 = Fly_On and Color3.fromRGB(50, 255, 50) or Color3.fromRGB(0, 170, 255)
	if Fly_On then StartFly() else StopFly() end
end)

print("S Logo ESP V9.2 FIX Loaded - by Rama")

end)

if not success then
	warn("S Hub Error: "..err)
end
